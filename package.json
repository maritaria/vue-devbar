{
  "private": true,
  "workspaces": [
    "demo-es6",
    "demo-typescript",
    "vue-devbar",
    "vue-devbar-decorators"
  ],
  "scripts": {
    "build": "yarn build:source && yarn build:demo",
    "build:demo": "yarn workspace demo-es6 build && yarn workspace demo-typescript build",
    "build:source": "yarn workspace vue-devbar-decorators build && yarn workspace vue-devbar build"
  }
}
